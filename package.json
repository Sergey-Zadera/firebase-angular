{
  "name": "firebase-angular",
  "version": "0.0.0",
  "description": "Firebase + AngularJS",
  "repository": "https://github.com/Sergey-Zadera/firebase-angular",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}